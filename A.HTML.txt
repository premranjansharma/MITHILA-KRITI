<!DOCTYPE html>
<html lang="hi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mithila Kriti Shop — मिथिला कृति</title>
<link href="https://fonts.googleapis.com/css2?family=Yatra+One&family=Tiro+Devanagari+Hindi:ital@0;1&family=DM+Serif+Display:ital@0;1&family=Crimson+Pro:ital,wght@0,300;0,400;0,600;1,300&display=swap" rel="stylesheet">
<style>
:root {
  --red: #C0392B;
  --gold: #C9920A;
  --black: #1A1008;
  --cream: #FDF6E3;
  --orange: #D4620A;
  --teal: #1A7A6E;
  --border: #8B4513;
  --light: #FAF0D0;
  --muted: #7a5c3a;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  background: var(--cream);
  color: var(--black);
  font-family: 'Crimson Pro', serif;
  cursor: default;
}

/* ── Top border ── */
.border-top {
  height: 8px;
  background: repeating-linear-gradient(90deg,
    var(--red) 0 16px, var(--gold) 16px 32px,
    var(--teal) 32px 48px, var(--orange) 48px 64px);
}

/* ── Announcement bar ── */
.announce {
  background: var(--black);
  color: var(--gold);
  text-align: center;
  padding: 0.55rem 1rem;
  font-size: 0.82rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  font-family: 'Crimson Pro', serif;
  font-style: italic;
}

/* ── Navbar ── */
nav {
  background: var(--cream);
  border-bottom: 1.5px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 3.5rem;
  position: sticky;
  top: 0;
  z-index: 200;
}

.nav-logo {
  font-family: 'Yatra One', cursive;
  font-size: 1.7rem;
  color: var(--red);
}
.nav-logo span { color: var(--teal); }

.nav-center {
  display: flex;
  gap: 2.5rem;
  list-style: none;
}
.nav-center a {
  font-size: 0.88rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--black);
  text-decoration: none;
  font-weight: 300;
  transition: color 0.25s;
}
.nav-center a:hover { color: var(--red); }

.nav-actions {
  display: flex;
  gap: 1.2rem;
  align-items: center;
}
.nav-btn {
  background: none;
  border: none;
  font-size: 1.3rem;
  cursor: pointer;
  color: var(--black);
  transition: color 0.25s;
  position: relative;
}
.nav-btn:hover { color: var(--red); }
.cart-count {
  position: absolute;
  top: -6px; right: -8px;
  background: var(--red);
  color: #fff;
  font-size: 0.6rem;
  width: 17px; height: 17px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-family: 'Crimson Pro', serif;
  font-weight: 600;
}

/* ── Hero Banner ── */
.hero-banner {
  background: var(--black);
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  min-height: 480px;
  overflow: hidden;
}

.hero-text {
  padding: 5rem 4rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.hero-badge {
  display: inline-block;
  background: var(--red);
  color: var(--cream);
  font-size: 0.72rem;
  letter-spacing: 4px;
  text-transform: uppercase;
  padding: 0.35rem 1rem;
  margin-bottom: 1.5rem;
  width: fit-content;
}

.hero-h1 {
  font-family: 'Yatra One', cursive;
  font-size: 4rem;
  color: var(--cream);
  line-height: 1.1;
  margin-bottom: 0.6rem;
}
.hero-h1 em {
  color: var(--gold);
  font-style: normal;
  display: block;
}

.hero-sub {
  font-family: 'DM Serif Display', serif;
  font-style: italic;
  color: #c8a86a;
  font-size: 1.2rem;
  margin-bottom: 1.5rem;
}

.hero-p {
  color: #a08060;
  font-size: 1rem;
  line-height: 1.8;
  max-width: 380px;
  font-weight: 300;
  margin-bottom: 2.5rem;
}

.btn-gold {
  display: inline-block;
  background: var(--gold);
  color: var(--black);
  padding: 0.9rem 2.5rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.9rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  text-decoration: none;
  font-weight: 600;
  transition: all 0.3s;
  border: none;
  cursor: pointer;
}
.btn-gold:hover { background: var(--cream); }

.hero-visual {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
.hero-visual svg {
  position: absolute;
  width: 100%; height: 100%;
  opacity: 0.15;
}
.hero-visual-center {
  position: relative;
  z-index: 2;
  text-align: center;
}
.hero-visual-center .big { font-family: 'Yatra One', cursive; font-size: 9rem; color: var(--gold); line-height: 1; }
.hero-visual-center .small { font-family: 'Tiro Devanagari Hindi', serif; color: var(--cream); opacity: 0.5; letter-spacing: 4px; font-size: 0.9rem; margin-top: 0.5rem; }

/* ── Category Filter ── */
.filter-bar {
  padding: 2rem 3.5rem 1rem;
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-wrap: wrap;
  border-bottom: 1px solid rgba(139,69,19,0.2);
}

.filter-label {
  font-size: 0.75rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--muted);
  margin-right: 0.5rem;
}

.filter-btn {
  background: none;
  border: 1px solid rgba(139,69,19,0.35);
  padding: 0.45rem 1.2rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.9rem;
  color: var(--muted);
  cursor: pointer;
  transition: all 0.25s;
  letter-spacing: 1px;
}
.filter-btn:hover, .filter-btn.active {
  background: var(--black);
  color: var(--gold);
  border-color: var(--black);
}

.sort-select {
  margin-left: auto;
  background: none;
  border: 1px solid rgba(139,69,19,0.35);
  padding: 0.45rem 1rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.9rem;
  color: var(--muted);
  cursor: pointer;
}

/* ── Product Grid ── */
.shop-section {
  padding: 2.5rem 3.5rem 4rem;
}

.shop-header {
  display: flex;
  align-items: baseline;
  gap: 1rem;
  margin-bottom: 2rem;
}

.shop-title {
  font-family: 'Yatra One', cursive;
  font-size: 2rem;
  color: var(--black);
}

.shop-count {
  font-size: 0.85rem;
  color: var(--muted);
  font-style: italic;
}

.products-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5px;
  background: rgba(139,69,19,0.15);
  border: 1.5px solid rgba(139,69,19,0.15);
}

.product-card {
  background: var(--cream);
  transition: transform 0.3s;
  position: relative;
  overflow: hidden;
}

.product-card:hover { transform: translateY(-4px); z-index: 2; box-shadow: 0 12px 40px rgba(26,16,8,0.18); }

.product-img {
  width: 100%;
  aspect-ratio: 3/3.5;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 5rem;
  position: relative;
  overflow: hidden;
}

.product-img .art-bg {
  position: absolute;
  inset: 0;
  opacity: 0.12;
}

.product-img .emoji { position: relative; z-index: 2; }

.badge-new {
  position: absolute;
  top: 12px; left: 12px;
  background: var(--teal);
  color: var(--cream);
  font-size: 0.62rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  padding: 3px 8px;
  z-index: 3;
}
.badge-sale {
  position: absolute;
  top: 12px; left: 12px;
  background: var(--red);
  color: var(--cream);
  font-size: 0.62rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  padding: 3px 8px;
  z-index: 3;
}

.wishlist-btn {
  position: absolute;
  top: 10px; right: 10px;
  background: rgba(253,246,227,0.85);
  border: none;
  width: 32px; height: 32px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 0.95rem;
  cursor: pointer;
  z-index: 3;
  transition: all 0.25s;
  opacity: 0;
}
.product-card:hover .wishlist-btn { opacity: 1; }
.wishlist-btn:hover { background: var(--red); color: #fff; }

.quick-add {
  position: absolute;
  bottom: 0; left: 0; right: 0;
  background: var(--black);
  color: var(--gold);
  border: none;
  padding: 0.7rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.8rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  cursor: pointer;
  transform: translateY(100%);
  transition: transform 0.3s;
  z-index: 3;
}
.product-card:hover .quick-add { transform: translateY(0); }
.quick-add:hover { background: var(--red); color: var(--cream); }

.product-info {
  padding: 1rem 1.1rem 1.3rem;
  border-top: 1px solid rgba(139,69,19,0.15);
}

.product-category {
  font-size: 0.7rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--teal);
  margin-bottom: 0.3rem;
}

.product-name {
  font-family: 'DM Serif Display', serif;
  font-size: 1.05rem;
  color: var(--black);
  margin-bottom: 0.5rem;
  line-height: 1.3;
}

.product-desc {
  font-size: 0.82rem;
  color: var(--muted);
  font-weight: 300;
  margin-bottom: 0.8rem;
  line-height: 1.5;
}

.price-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.5rem;
}

.price {
  font-family: 'DM Serif Display', serif;
  font-size: 1.3rem;
  color: var(--black);
}

.price-old {
  font-size: 0.85rem;
  color: var(--muted);
  text-decoration: line-through;
  margin-left: 0.3rem;
}

.rating {
  font-size: 0.75rem;
  color: var(--gold);
  letter-spacing: 1px;
}

/* ── Banner strip ── */
.mid-banner {
  background: var(--red);
  padding: 3rem 3.5rem;
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 2rem;
  margin: 2rem 0;
}

.mid-banner-item {
  text-align: center;
  color: var(--cream);
  border-right: 1px solid rgba(253,246,227,0.25);
  padding-right: 2rem;
}
.mid-banner-item:last-child { border-right: none; }

.mid-banner-icon { font-size: 2rem; margin-bottom: 0.7rem; }
.mid-banner-title {
  font-family: 'DM Serif Display', serif;
  font-size: 1.1rem;
  margin-bottom: 0.3rem;
}
.mid-banner-desc { font-size: 0.85rem; opacity: 0.75; font-weight: 300; }

/* ── Featured product ── */
.featured {
  display: grid;
  grid-template-columns: 1fr 1fr;
  background: var(--light);
  border-top: 1.5px solid rgba(139,69,19,0.2);
  border-bottom: 1.5px solid rgba(139,69,19,0.2);
}

.featured-img {
  background: var(--black);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 10rem;
  min-height: 420px;
  position: relative;
  overflow: hidden;
}
.featured-img svg { position: absolute; width: 100%; height: 100%; opacity: 0.1; }
.featured-img .fi-emoji { position: relative; z-index: 2; }

.featured-content {
  padding: 4rem 3.5rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.featured-label {
  font-size: 0.72rem;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--red);
  margin-bottom: 1rem;
}

.featured-title {
  font-family: 'Yatra One', cursive;
  font-size: 2.5rem;
  color: var(--black);
  margin-bottom: 0.5rem;
  line-height: 1.2;
}

.featured-desc {
  font-size: 1rem;
  line-height: 1.9;
  color: var(--muted);
  font-weight: 300;
  margin-bottom: 1.5rem;
  max-width: 380px;
}

.featured-price {
  font-family: 'DM Serif Display', serif;
  font-size: 2.2rem;
  color: var(--black);
  margin-bottom: 2rem;
}
.featured-price span { font-size: 1rem; color: var(--muted); font-family: 'Crimson Pro', serif; text-decoration: line-through; margin-left: 0.8rem; }

.btn-dark {
  display: inline-block;
  background: var(--black);
  color: var(--cream);
  padding: 1rem 2.5rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.9rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  text-decoration: none;
  border: none;
  cursor: pointer;
  transition: all 0.3s;
  margin-right: 1rem;
}
.btn-dark:hover { background: var(--red); }

.btn-ghost {
  display: inline-block;
  background: transparent;
  color: var(--black);
  padding: 1rem 2rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.9rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  text-decoration: none;
  border: 1.5px solid var(--black);
  cursor: pointer;
  transition: all 0.3s;
}
.btn-ghost:hover { background: var(--black); color: var(--cream); }

/* ── Cart Drawer ── */
.cart-overlay {
  position: fixed;
  inset: 0;
  background: rgba(26,16,8,0.5);
  z-index: 500;
  display: none;
  align-items: flex-start;
  justify-content: flex-end;
}
.cart-overlay.open { display: flex; }

.cart-drawer {
  background: var(--cream);
  width: 380px;
  height: 100vh;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  animation: slideIn 0.3s ease;
}

@keyframes slideIn {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}

.cart-header {
  padding: 1.5rem 1.5rem 1rem;
  border-bottom: 1px solid rgba(139,69,19,0.2);
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.cart-header h2 { font-family: 'Yatra One', cursive; font-size: 1.4rem; color: var(--black); }
.cart-close { background: none; border: none; font-size: 1.4rem; cursor: pointer; color: var(--muted); }

.cart-items { flex: 1; padding: 1.5rem; }

.cart-item {
  display: flex;
  gap: 1rem;
  padding: 1rem 0;
  border-bottom: 1px solid rgba(139,69,19,0.1);
}

.cart-item-img {
  width: 70px; height: 70px;
  background: var(--black);
  display: flex; align-items: center; justify-content: center;
  font-size: 2rem;
  flex-shrink: 0;
}

.cart-item-info { flex: 1; }
.cart-item-name { font-family: 'DM Serif Display', serif; font-size: 0.95rem; margin-bottom: 0.3rem; }
.cart-item-price { font-size: 0.9rem; color: var(--red); font-weight: 600; }
.cart-item-qty {
  display: flex; align-items: center; gap: 0.5rem;
  margin-top: 0.5rem;
}
.qty-btn {
  width: 24px; height: 24px;
  background: none;
  border: 1px solid var(--border);
  cursor: pointer;
  font-size: 0.9rem;
  display: flex; align-items: center; justify-content: center;
  color: var(--black);
  transition: all 0.2s;
}
.qty-btn:hover { background: var(--black); color: var(--cream); }
.qty-num { font-size: 0.9rem; min-width: 20px; text-align: center; }

.cart-footer {
  padding: 1.5rem;
  border-top: 1.5px solid rgba(139,69,19,0.2);
  background: var(--light);
}
.cart-total-row { display: flex; justify-content: space-between; margin-bottom: 1.2rem; }
.cart-total-label { font-size: 0.85rem; color: var(--muted); letter-spacing: 2px; text-transform: uppercase; }
.cart-total-price { font-family: 'DM Serif Display', serif; font-size: 1.4rem; color: var(--black); }
.checkout-btn {
  width: 100%;
  background: var(--black);
  color: var(--gold);
  border: none;
  padding: 1rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.9rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  cursor: pointer;
  transition: background 0.3s;
}
.checkout-btn:hover { background: var(--red); color: var(--cream); }

/* ── Newsletter ── */
.newsletter {
  background: var(--black);
  padding: 4rem 3.5rem;
  text-align: center;
}
.newsletter-title {
  font-family: 'Yatra One', cursive;
  font-size: 2.2rem;
  color: var(--gold);
  margin-bottom: 0.8rem;
}
.newsletter-sub {
  font-size: 1rem;
  color: #9a8060;
  font-weight: 300;
  margin-bottom: 2rem;
  font-style: italic;
}
.newsletter-form {
  display: flex;
  max-width: 460px;
  margin: 0 auto;
  gap: 0;
}
.newsletter-input {
  flex: 1;
  background: rgba(253,246,227,0.08);
  border: 1px solid rgba(201,146,10,0.4);
  border-right: none;
  padding: 0.9rem 1.2rem;
  font-family: 'Crimson Pro', serif;
  font-size: 1rem;
  color: var(--cream);
  outline: none;
}
.newsletter-input::placeholder { color: #6a5a3a; }
.newsletter-submit {
  background: var(--gold);
  border: none;
  padding: 0.9rem 1.8rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.85rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--black);
  font-weight: 600;
  cursor: pointer;
  transition: background 0.3s;
}
.newsletter-submit:hover { background: var(--cream); }

/* ── Footer ── */
.footer {
  background: #120c05;
  padding: 3.5rem 3.5rem 2rem;
}
.footer-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 3rem;
  padding-bottom: 2.5rem;
  border-bottom: 1px solid rgba(201,146,10,0.15);
  margin-bottom: 2rem;
}
.footer-brand .logo {
  font-family: 'Yatra One', cursive;
  font-size: 1.6rem;
  color: var(--gold);
  margin-bottom: 1rem;
}
.footer-brand p { font-size: 0.88rem; color: #7a6040; line-height: 1.8; font-weight: 300; max-width: 220px; }
.footer-col h4 { font-size: 0.72rem; letter-spacing: 3px; text-transform: uppercase; color: var(--gold); margin-bottom: 1.2rem; }
.footer-col ul { list-style: none; }
.footer-col li { margin-bottom: 0.6rem; }
.footer-col a { font-size: 0.88rem; color: #6a5030; text-decoration: none; transition: color 0.2s; font-weight: 300; }
.footer-col a:hover { color: var(--cream); }
.footer-bottom { display: flex; justify-content: space-between; align-items: center; }
.footer-copy { font-size: 0.78rem; color: #4a3820; }
.footer-pay { display: flex; gap: 0.6rem; }
.pay-badge {
  background: rgba(201,146,10,0.12);
  border: 1px solid rgba(201,146,10,0.2);
  padding: 0.25rem 0.7rem;
  font-size: 0.7rem;
  color: #8a7040;
  letter-spacing: 1px;
}

/* ── Toast ── */
.toast {
  position: fixed;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%) translateY(100px);
  background: var(--teal);
  color: var(--cream);
  padding: 0.9rem 2rem;
  font-family: 'Crimson Pro', serif;
  font-size: 0.95rem;
  letter-spacing: 1px;
  z-index: 999;
  transition: transform 0.4s ease;
  white-space: nowrap;
}
.toast.show { transform: translateX(-50%) translateY(0); }

/* ── Animations ── */
@keyframes fadeUp { from { opacity:0; transform:translateY(28px); } to { opacity:1; transform:translateY(0); } }
.hero-badge { animation: fadeUp 0.5s ease both; }
.hero-h1 { animation: fadeUp 0.6s 0.08s ease both; }
.hero-sub { animation: fadeUp 0.6s 0.16s ease both; }
.hero-p { animation: fadeUp 0.6s 0.24s ease both; }
.btn-gold { animation: fadeUp 0.6s 0.32s ease both; }

@media(max-width: 900px){
  nav { padding: 1rem 1.5rem; }
  .hero-banner { grid-template-columns: 1fr; }
  .hero-visual { min-height: 240px; }
  .hero-text { padding: 3rem 1.5rem; }
  .hero-h1 { font-size: 2.8rem; }
  .filter-bar, .shop-section { padding-left: 1.5rem; padding-right: 1.5rem; }
  .products-grid { grid-template-columns: repeat(2, 1fr); }
  .featured { grid-template-columns: 1fr; }
  .featured-img { min-height: 260px; }
  .featured-content { padding: 2.5rem 1.5rem; }
  .mid-banner { grid-template-columns: 1fr; padding: 2rem 1.5rem; }
  .mid-banner-item { border-right: none; border-bottom: 1px solid rgba(253,246,227,0.15); padding-bottom: 1.5rem; }
  .footer-grid { grid-template-columns: 1fr 1fr; padding-left: 1.5rem; }
  .footer { padding: 2.5rem 1.5rem 1.5rem; }
  .cart-drawer { width: 100vw; }
  .newsletter { padding: 3rem 1.5rem; }
  .newsletter-form { flex-direction: column; }
  .newsletter-input { border-right: 1px solid rgba(201,146,10,0.4); }
}
</style>
</head>
<body>

<!-- Top accent -->
<div class="border-top"></div>
<div class="announce">✦ Free Shipping on orders above ₹999 across India ✦ Handcrafted with Love from Mithila ✦</div>

<!-- Nav -->
<nav>
  <div class="nav-logo">Mithila <span>Kriti</span></div>
  <ul class="nav-center">
    <li><a href="#">Paintings</a></li>
    <li><a href="#">Prints</a></li>
    <li><a href="#">Gifts</a></li>
    <li><a href="#">Workshop</a></li>
    <li><a href="#">About</a></li>
  </ul>
  <div class="nav-actions">
    <button class="nav-btn" title="Search">🔍</button>
    <button class="nav-btn" title="Wishlist">🤍</button>
    <button class="nav-btn" onclick="openCart()" title="Cart">
      🛒
      <span class="cart-count" id="cartCount">0</span>
    </button>
  </div>
</nav>

<!-- Hero -->
<section class="hero-banner">
  <div class="hero-text">
    <span class="hero-badge">✦ New Collection 2026</span>
    <h1 class="hero-h1">Authentic<em>Madhubani Art</em></h1>
    <p class="hero-sub">— Bihar ki Pavitra Dharohar —</p>
    <p class="hero-p">Hastanirmit paintings, prints aur gifts — seedhe Mithila ke kalaakaron se aapke ghar tak. Har piece ek jeevit parampara ka hissa hai.</p>
    <a href="#shop" class="btn-gold">Shop Now</a>
  </div>
  <div class="hero-visual">
    <svg viewBox="0 0 600 500" xmlns="http://www.w3.org/2000/svg">
      <circle cx="300" cy="250" r="240" stroke="#C9920A" stroke-width="1.5" fill="none"/>
      <circle cx="300" cy="250" r="190" stroke="#C0392B" stroke-width="1" fill="none"/>
      <circle cx="300" cy="250" r="140" stroke="#1A7A6E" stroke-width="1.5" fill="none"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#C9920A" opacity="0.7"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#C0392B" opacity="0.5" transform="rotate(45 300 250)"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#1A7A6E" opacity="0.5" transform="rotate(90 300 250)"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#C9920A" opacity="0.5" transform="rotate(135 300 250)"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#C0392B" opacity="0.5" transform="rotate(180 300 250)"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#1A7A6E" opacity="0.5" transform="rotate(225 300 250)"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#C9920A" opacity="0.5" transform="rotate(270 300 250)"/>
      <ellipse cx="300" cy="120" rx="20" ry="45" fill="#C0392B" opacity="0.5" transform="rotate(315 300 250)"/>
      <circle cx="300" cy="250" r="10" fill="#C0392B"/>
    </svg>
    <div class="hero-visual-center">
      <div class="big">🎨</div>
      <div class="small">मधुबनी · कला · परंपरा</div>
    </div>
  </div>
</section>

<!-- Filter Bar -->
<div class="filter-bar" id="shop">
  <span class="filter-label">Filter:</span>
  <button class="filter-btn active" onclick="filterProducts('all', this)">Sabhi</button>
  <button class="filter-btn" onclick="filterProducts('painting', this)">Paintings</button>
  <button class="filter-btn" onclick="filterProducts('print', this)">Prints</button>
  <button class="filter-btn" onclick="filterProducts('gift', this)">Gifts</button>
  <button class="filter-btn" onclick="filterProducts('workshop', this)">Workshop</button>
  <select class="sort-select" onchange="sortProducts(this.value)">
    <option value="">Sort by</option>
    <option value="low">Price: Low to High</option>
    <option value="high">Price: High to Low</option>
    <option value="new">Newest First</option>
  </select>
</div>

<!-- Product Shop -->
<section class="shop-section">
  <div class="shop-header">
    <h2 class="shop-title">Hamaari Dukaan</h2>
    <span class="shop-count" id="productCount">12 products</span>
  </div>
  <div class="products-grid" id="productsGrid">
    <!-- Products injected by JS -->
  </div>
</section>

<!-- Mid Banner -->
<div class="mid-banner">
  <div class="mid-banner-item">
    <div class="mid-banner-icon">🚚</div>
    <div class="mid-banner-title">Free Delivery</div>
    <div class="mid-banner-desc">₹999 se upar ke orders par free shipping across India</div>
  </div>
  <div class="mid-banner-item">
    <div class="mid-banner-icon">🎨</div>
    <div class="mid-banner-title">100% Handmade</div>
    <div class="mid-banner-desc">Seedhe Mithila ke certified kalaakaron dwara nirmit</div>
  </div>
  <div class="mid-banner-item">
    <div class="mid-banner-icon">↩️</div>
    <div class="mid-banner-title">Easy Returns</div>
    <div class="mid-banner-desc">7-din return policy — koi sawaal nahi</div>
  </div>
</div>

<!-- Featured Product -->
<section class="featured">
  <div class="featured-img">
    <svg viewBox="0 0 500 420" xmlns="http://www.w3.org/2000/svg">
      <circle cx="250" cy="210" r="200" stroke="#C9920A" stroke-width="2" fill="none"/>
      <circle cx="250" cy="210" r="150" stroke="#C0392B" stroke-width="1.5" fill="none"/>
      <ellipse cx="250" cy="80" rx="18" ry="40" fill="#C9920A" opacity="0.8"/>
      <ellipse cx="250" cy="80" rx="18" ry="40" fill="#C0392B" opacity="0.6" transform="rotate(60 250 210)"/>
      <ellipse cx="250" cy="80" rx="18" ry="40" fill="#1A7A6E" opacity="0.6" transform="rotate(120 250 210)"/>
      <ellipse cx="250" cy="80" rx="18" ry="40" fill="#C9920A" opacity="0.6" transform="rotate(180 250 210)"/>
      <ellipse cx="250" cy="80" rx="18" ry="40" fill="#C0392B" opacity="0.6" transform="rotate(240 250 210)"/>
      <ellipse cx="250" cy="80" rx="18" ry="40" fill="#1A7A6E" opacity="0.6" transform="rotate(300 250 210)"/>
    </svg>
    <span class="fi-emoji">🦚</span>
  </div>
  <div class="featured-content">
    <p class="featured-label">✦ Editor's Pick — Featured Artwork</p>
    <h2 class="featured-title">Peacock Madhubani — Large Canvas</h2>
    <p class="featured-desc">
      48×36 inch hand-painted canvas. Mithila ki iconic mor painting — natural rang aur bamboo brush se bani. GI-certified kalakar dwara nirmit. Certificate of Authenticity ke saath.
    </p>
    <div class="featured-price">₹8,500 <span>₹11,000</span></div>
    <div>
      <button class="btn-dark" onclick="addToCart('Peacock Madhubani Canvas', 8500, '🦚')">Add to Cart</button>
      <a href="#" class="btn-ghost">Details</a>
    </div>
  </div>
</section>

<!-- Newsletter -->
<section class="newsletter">
  <h2 class="newsletter-title">नई कृतियों की खबर पाएं</h2>
  <p class="newsletter-sub">New launches, exclusive offers aur kalakar ki stories — seedhe aapke inbox mein</p>
  <div class="newsletter-form">
    <input type="email" class="newsletter-input" placeholder="Aapka email address...">
    <button class="newsletter-submit" onclick="showToast('Shukriya! Aap subscribe ho gaye ✦')">Subscribe</button>
  </div>
</section>

<!-- Footer -->
<footer class="footer">
  <div class="footer-grid">
    <div class="footer-brand">
      <div class="logo">Mithila Kriti</div>
      <p>Bihar ki pavitra Madhubani kala ko duniya tak pahunchane ka ek prayas. Har rekha mein ek kahani.</p>
    </div>
    <div class="footer-col">
      <h4>Shop</h4>
      <ul>
        <li><a href="#">Paintings</a></li>
        <li><a href="#">Prints & Posters</a></li>
        <li><a href="#">Gift Sets</a></li>
        <li><a href="#">Workshop Kits</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Help</h4>
      <ul>
        <li><a href="#">Shipping Info</a></li>
        <li><a href="#">Returns</a></li>
        <li><a href="#">Track Order</a></li>
        <li><a href="#">Contact Us</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Connect</h4>
      <ul>
        <li><a href="#">Instagram</a></li>
        <li><a href="#">Facebook</a></li>
        <li><a href="#">YouTube</a></li>
        <li><a href="#">WhatsApp</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <div class="footer-copy">© 2026 Mithila Kriti. Sarvaadhikar Surakshit.</div>
    <div class="footer-pay">
      <span class="pay-badge">UPI</span>
      <span class="pay-badge">VISA</span>
      <span class="pay-badge">MASTERCARD</span>
      <span class="pay-badge">COD</span>
    </div>
  </div>
</footer>

<!-- Cart Drawer -->
<div class="cart-overlay" id="cartOverlay" onclick="closeCartOutside(event)">
  <div class="cart-drawer">
    <div class="cart-header">
      <h2>🛒 Aapki Cart</h2>
      <button class="cart-close" onclick="closeCart()">✕</button>
    </div>
    <div class="cart-items" id="cartItems">
      <p style="color:var(--muted); font-style:italic; text-align:center; margin-top:3rem; font-size:0.95rem;">Cart khaali hai — kuch chunein! 🎨</p>
    </div>
    <div class="cart-footer">
      <div class="cart-total-row">
        <span class="cart-total-label">Total</span>
        <span class="cart-total-price" id="cartTotal">₹0</span>
      </div>
      <button class="checkout-btn" onclick="showToast('Checkout jald aayega! ✦')">Checkout Karen →</button>
    </div>
  </div>
</div>

<!-- Toast -->
<div class="toast" id="toast"></div>

<script>
// Products data
const products = [
  { id:1, name:'Ganesha Madhubani', cat:'painting', emoji:'🐘', price:3200, old:4500, rating:'★★★★★', desc:'18×24 inch, canvas pe handmade', badge:'new', new:true },
  { id:2, name:'Fish Pair (Machh)', cat:'painting', emoji:'🐠', price:1800, old:null, rating:'★★★★☆', desc:'12×16 inch, kagaz pe', badge:null, new:false },
  { id:3, name:'Lotus Mandala Print', cat:'print', emoji:'🌸', price:450, old:600, rating:'★★★★★', desc:'A3 archival print, framed', badge:'sale', new:false },
  { id:4, name:'Durga Mata Canvas', cat:'painting', emoji:'🌺', price:5500, old:7000, rating:'★★★★★', desc:'24×36 inch, hand-painted', badge:'sale', new:false },
  { id:5, name:'Mithila Art Gift Box', cat:'gift', emoji:'🎁', price:1299, old:null, rating:'★★★★☆', desc:'4 prints + notepad set', badge:'new', new:true },
  { id:6, name:'Bamboo Brush Kit', cat:'workshop', emoji:'🖌️', price:699, old:899, rating:'★★★★★', desc:'Professional set, 12 brushes', badge:null, new:false },
  { id:7, name:'Krishna Leela Panel', cat:'painting', emoji:'🦋', price:6800, old:9000, rating:'★★★★★', desc:'30×40 inch, triptych set', badge:'new', new:true },
  { id:8, name:'Peacock Poster Set', cat:'print', emoji:'🦚', price:380, old:null, rating:'★★★★☆', desc:'Set of 3, A4 prints', badge:null, new:false },
  { id:9, name:'Mithila Color Set', cat:'workshop', emoji:'🎨', price:549, old:750, rating:'★★★★★', desc:'Natural pigment colors, 12 shades', badge:'sale', new:false },
  { id:10, name:'Wedding Souvenir Set', cat:'gift', emoji:'💐', price:2100, old:null, rating:'★★★★★', desc:'Custom name + 6 mini paintings', badge:'new', new:true },
  { id:11, name:'Village Scene Canvas', cat:'painting', emoji:'🌿', price:4200, old:5500, rating:'★★★★☆', desc:'20×30 inch, detailed village art', badge:null, new:false },
  { id:12, name:'Owl (Ullu) Print', cat:'print', emoji:'🦉', price:320, old:400, rating:'★★★★★', desc:'A3 giclée print, unframed', badge:'sale', new:false },
];

let cart = [];
let currentFilter = 'all';

// Render products
function renderProducts(items) {
  const grid = document.getElementById('productsGrid');
  document.getElementById('productCount').textContent = items.length + ' products';
  grid.innerHTML = items.map(p => `
    <div class="product-card" data-cat="${p.cat}">
      <div class="product-img" style="background:${getColor(p.id)}">
        <svg viewBox="0 0 200 220" class="art-bg" xmlns="http://www.w3.org/2000/svg">
          <circle cx="100" cy="110" r="90" stroke="#fff" stroke-width="1" fill="none"/>
          <circle cx="100" cy="110" r="60" stroke="#fff" stroke-width="0.8" fill="none"/>
          <circle cx="100" cy="110" r="30" stroke="#fff" stroke-width="0.8" fill="none"/>
        </svg>
        <span class="emoji">${p.emoji}</span>
        ${p.badge === 'new' ? '<span class="badge-new">New</span>' : ''}
        ${p.badge === 'sale' ? '<span class="badge-sale">Sale</span>' : ''}
        <button class="wishlist-btn" onclick="showToast('Wishlist mein add kiya! 🤍')">🤍</button>
        <button class="quick-add" onclick="addToCart('${p.name}', ${p.price}, '${p.emoji}')">+ Cart Mein Daalen</button>
      </div>
      <div class="product-info">
        <p class="product-category">${p.cat}</p>
        <h3 class="product-name">${p.name}</h3>
        <p class="product-desc">${p.desc}</p>
        <div class="price-row">
          <div>
            <span class="price">₹${p.price.toLocaleString('en-IN')}</span>
            ${p.old ? `<span class="price-old">₹${p.old.toLocaleString('en-IN')}</span>` : ''}
          </div>
          <span class="rating">${p.rating}</span>
        </div>
      </div>
    </div>
  `).join('');
}

function getColor(id) {
  const colors = ['#3D1A0A','#0A3D2E','#3D2A0A','#1A0A3D','#3D0A1A','#0A2A3D','#2A3D0A','#3D1A2A','#0A3D1A','#2A0A3D','#3D2A1A','#0A1A3D'];
  return colors[(id-1) % colors.length];
}

// Filter
function filterProducts(cat, btn) {
  currentFilter = cat;
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  const filtered = cat === 'all' ? products : products.filter(p => p.cat === cat);
  renderProducts(filtered);
}

// Sort
function sortProducts(val) {
  let items = currentFilter === 'all' ? [...products] : products.filter(p => p.cat === currentFilter);
  if (val === 'low') items.sort((a,b) => a.price - b.price);
  if (val === 'high') items.sort((a,b) => b.price - a.price);
  if (val === 'new') items.sort((a,b) => b.new - a.new);
  renderProducts(items);
}

// Cart
function addToCart(name, price, emoji) {
  const existing = cart.find(i => i.name === name);
  if (existing) { existing.qty++; }
  else { cart.push({ name, price, emoji, qty: 1 }); }
  updateCart();
  openCart();
  showToast('✦ Cart mein add ho gaya!');
}

function updateCart() {
  const count = cart.reduce((s,i) => s + i.qty, 0);
  document.getElementById('cartCount').textContent = count;
  const total = cart.reduce((s,i) => s + i.price * i.qty, 0);
  document.getElementById('cartTotal').textContent = '₹' + total.toLocaleString('en-IN');
  const container = document.getElementById('cartItems');
  if (cart.length === 0) {
    container.innerHTML = '<p style="color:var(--muted);font-style:italic;text-align:center;margin-top:3rem;font-size:0.95rem;">Cart khaali hai — kuch chunein! 🎨</p>';
  } else {
    container.innerHTML = cart.map((item, idx) => `
      <div class="cart-item">
        <div class="cart-item-img">${item.emoji}</div>
        <div class="cart-item-info">
          <div class="cart-item-name">${item.name}</div>
          <div class="cart-item-price">₹${item.price.toLocaleString('en-IN')}</div>
          <div class="cart-item-qty">
            <button class="qty-btn" onclick="changeQty(${idx}, -1)">−</button>
            <span class="qty-num">${item.qty}</span>
            <button class="qty-btn" onclick="changeQty(${idx}, 1)">+</button>
          </div>
        </div>
      </div>
    `).join('');
  }
}

function changeQty(idx, delta) {
  cart[idx].qty += delta;
  if (cart[idx].qty <= 0) cart.splice(idx, 1);
  updateCart();
}

function openCart() { document.getElementById('cartOverlay').classList.add('open'); }
function closeCart() { document.getElementById('cartOverlay').classList.remove('open'); }
function closeCartOutside(e) { if (e.target === document.getElementById('cartOverlay')) closeCart(); }

// Toast
let toastTimer;
function showToast(msg) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => t.classList.remove('show'), 2500);
}

// Init
renderProducts(products);
</script>
</body>
</html>
